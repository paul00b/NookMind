<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.NookMind" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>

    <!-- Splash screen (androidx.core:core-splashscreen), replaces the Capacitor launch theme. -->
    <style name="Theme.NookMind.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.NookMind</item>
    </style>
</resources>
